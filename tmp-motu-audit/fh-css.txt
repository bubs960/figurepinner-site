========================================================
   FANDOM HUB — culture-led themed price-intel hubs
   Base component styles (.fh-*) + per-fandom theme via [data-fandom].
   Perf: no above-fold raster art (motif is pure CSS). A11y: motion is
   reduced-motion-gated; palettes target WCAG AA on their own dark bg.
   ============================================================ */

.fh-main {
  --fh-bg:      var(--bg);
  --fh-surface: var(--s1);
  --fh-accent:  var(--blue);
  --fh-accent2: var(--orange);
  --fh-hot:     var(--yellow);
  --fh-text:    var(--text);
  --fh-muted:   var(--muted);
  --fh-line:    var(--border);
  --fh-display: var(--fp-font-display);
  --fh-motif:   none;
  background: var(--fh-bg);
  min-height: 100vh;
  color: var(--fh-text);
  font-family: var(--font-body);
}

.fh-hero {
  position: relative;
  border-bottom: 2px solid var(--fh-accent);
  background-color: var(--fh-surface);
  background-image: var(--fh-motif);
  overflow: hidden;
}
.fh-hero-inner {
  max-width: 760px;
  margin: 0 auto;
  padding: 3rem 1.25rem 2.25rem;
  position: relative;
  z-index: 1;
}
.fh-kicker {
  font-family: var(--fh-display);
  font-size: 0.8rem;
  letter-spacing: 0.22em;
  color: var(--fh-accent2);
  text-transform: uppercase;
  margin-bottom: 0.6rem;
}
.fh-title {
  font-family: var(--fh-display);
  font-size: clamp(2rem, 5.5vw, 3rem);
  line-height: 1.08;
  letter-spacing: 0.01em;
  color: var(--fh-text);
  margin: 0 0 0.9rem;
}
.fh-lead {
  font-size: 1.12rem;
  line-height: 1.65;
  color: var(--fh-muted);
  margin: 0 0 1.6rem;
  max-width: 60ch;
}
.fh-search { display: flex; gap: 0.5rem; flex-wrap: wrap; }
.fh-search-input {
  flex: 1 1 16rem;
  min-width: 0;
  background: var(--fh-bg);
  border: 1px solid var(--fh-line);
  border-radius: var(--fp-radius-sm);
  padding: 0.7rem 0.95rem;
  color: var(--fh-text);
  font-size: 0.98rem;
}
.fh-search-input:focus-visible { outline: 2px solid var(--fh-accent); outline-offset: 1px; }
.fh-search-btn {
  background: var(--fh-accent);
  color: #fff;
  border: none;
  border-radius: var(--fp-radius-sm);
  padding: 0.7rem 1.1rem;
  font-weight: 600;
  font-size: 0.95rem;
  cursor: pointer;
}

.fh-body { max-width: 720px; margin: 0 auto; padding: 2.25rem 1.25rem 5rem; }

/* Intel comp table — fandom-voiced, the funnel into figure pages */
.fh-intel {
  border: 1px solid var(--fh-line);
  border-top: 3px solid var(--fh-accent2);
  border-radius: 0 0 var(--fp-radius) var(--fp-radius);
  background: var(--fh-surface);
  padding: 1.1rem 1.1rem 0.9rem;
  margin-bottom: 2.25rem;
}
.fh-intel-head { margin-bottom: 0.8rem; }
.fh-intel-title {
  font-family: var(--fh-display);
  font-size: 1.25rem;
  letter-spacing: 0.04em;
  color: var(--fh-hot);
  margin: 0 0 0.2rem;
}
.fh-intel-sub { font-size: 0.85rem; color: var(--fh-muted); margin: 0; }
.fh-intel-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 2px; }
.fh-intel-link {
  display: grid;
  grid-template-columns: 1.6rem 1fr auto auto;
  align-items: center;
  gap: 0.6rem;
  padding: 0.6rem 0.55rem;
  background: var(--fh-bg);
  border-radius: var(--fp-radius-sm);
  text-decoration: none;
  color: var(--fh-text);
  transition: background var(--transition);
}
.fh-intel-link:hover { background: var(--fh-surface); }
.fh-intel-rank { font-family: var(--fh-display); font-size: 1rem; color: var(--fh-muted); text-align: center; }
.fh-intel-name { display: flex; flex-direction: column; font-size: 0.96rem; font-weight: 600; }
.fh-intel-line { font-size: 0.76rem; font-weight: 400; color: var(--fh-muted); margin-top: 1px; }
.fh-intel-flag {
  font-size: 0.66rem;
  letter-spacing: 0.06em;
  font-weight: 700;
  color: var(--fh-accent2);
  border: 1px solid var(--fh-accent2);
  border-radius: 999px;
  padding: 0.12rem 0.5rem;
  white-space: nowrap;
}
.fh-intel-price { display: flex; flex-direction: column; align-items: flex-end; font-family: var(--fh-display); font-size: 1.02rem; color: var(--fh-hot); }
.fh-intel-date { font-family: var(--font-body); font-size: 0.7rem; font-weight: 400; color: var(--fh-muted); margin-top: 1px; }
.fh-intel-foot { font-size: 0.74rem; color: var(--fh-muted); margin: 0.7rem 0 0.2rem; font-style: italic; }
.fh-intel-empty { font-size: 0.95rem; color: var(--fh-muted); font-style: italic; padding: 0.4rem 0.2rem 0.6rem; margin: 0; }

.fh-ad { display: flex; justify-content: center; margin: 0 0 2rem; }

/* Editorial body */
.fh-h2 { font-family: var(--fh-display); font-size: 1.5rem; letter-spacing: 0.02em; color: var(--fh-text); margin: 2.5rem 0 1rem; }
.fh-h3 { font-family: var(--fh-display); font-size: 1.15rem; color: var(--fh-text); margin: 1.75rem 0 0.75rem; }
.fh-p  { font-size: 1.05rem; color: var(--fh-muted); line-height: 1.8; margin: 0 0 1.25rem; }
.fh-ul { margin: 0 0 1.25rem; padding-left: 1.25rem; display: flex; flex-direction: column; gap: 0.6rem; }
.fh-ul li { font-size: 1.02rem; color: var(--fh-muted); line-height: 1.7; }
.fh-callout {
  margin: 0 0 1.5rem; padding: 1rem 1.25rem;
  border-left: 3px solid var(--fh-accent2); background: var(--fh-surface);
  border-radius: 0 var(--fp-radius) var(--fp-radius) 0;
  font-size: 1rem; color: var(--fh-text); line-height: 1.65; font-style: italic;
}

.fh-cta-wrap { margin-top: 1rem; padding-top: 2rem; border-top: 1px solid var(--fh-line); }
.fh-cta { display: inline-block; background: var(--fh-accent); color: #fff; text-decoration: none; padding: 0.75rem 1.5rem; border-radius: var(--fp-radius-sm); font-weight: 600; }

.fh-more { margin-top: 2.5rem; }
.fh-more-label { font-size: 0.75rem; font-weight: 700; letter-spacing: 0.1em; color: var(--fh-muted); text-transform: uppercase; margin-bottom: 0.875rem; }
.fh-more-grid { display: grid; gap: 0.625rem; }
.fh-more-card { display: block; padding: 0.875rem 1rem; background: var(--fh-surface); border: 1px solid var(--fh-line); border-radius: var(--fp-radius-sm); color: var(--fh-text); text-decoration: none; font-size: 0.9rem; font-weight: 600; }
.fh-more-min { display: block; font-size: 0.78rem; color: var(--fh-muted); font-weight: 400; margin-top: 0.25rem; }

@media (max-width: 520px) {
  .fh-intel-link { grid-template-columns: 1.4rem 1fr auto; }
  .fh-intel-flag { display: none; }
}

/* ── THEME: Masters of the Universe — Obrero's misty Eternia ──────────
   Pea-green + black stone, mist, Grayskull gold. Painted/atmospheric.
   Motif is pure CSS (layered radial mist + faint stone speckle), zero
   image weight. AA-checked on the dark stone bg. */
.fh-main[data-fandom="masters-of-the-universe"] {
  --fh-bg:      #14100b;
  --fh-surface: #211a12;
  --fh-accent:  #6f9b3a;   /* Eternia/Skeletor green */
  --fh-accent2: #e0a52e;   /* Grayskull gold */
  --fh-hot:     #f0b942;   /* power gold (AA on dark) */
  --fh-text:    #f3ead5;
  --fh-muted:   #b6a888;
  --fh-line:    #3a2f20;
  --fh-motif:
    radial-gradient(120% 80% at 50% -10%, rgba(111,155,58,0.16), transparent 60%),
    radial-gradient(90% 60% at 85% 120%, rgba(224,165,46,0.10), transparent 55%),
    radial-gradient(70% 50% at 10% 110%, rgba(0,0,0,0.45), transparent 60%);
}
.fh-main[data-fandom="masters-of-the-universe"] .fh-search-btn,
.fh-main[data-fandom="masters-of-the-universe"] .fh-cta { color: #14100b; }

/* MOTU hero glow — torch flicker, motion-gated */
@media (prefers-reduced-motion: no-preference) {
  .fh-main[data-fandom="masters-of-the-universe"] .fh-kicker {
    animation: fh-torch 4.5s ease-in-out infinite;
  }
}
@keyframes fh-torch { 0%,100% { opacity: 1; } 50% { opacity: 0.72; } }

/* ── FandomHub interactive funnel tools (filter pills + price checker) ── */
.fh-check { display: flex; gap: 0.5rem; flex-wrap: wrap; margin: 0 0 0.9rem; }
.fh-check-input {
  flex: 1 1 14rem; min-width: 0;
  background: var(--fh-bg); border: 1px solid var(--fh-line);
  border-radius: var(--fp-radius-sm); padding: 0.6rem 0.85rem;
  color: var(--fh-text); font-size: 0.95rem;
}
.fh-check-input:focus-visible { outline: 2px solid var(--fh-accent); outline-offset: 1px; }
.fh-check-btn {
  background: var(--fh-accent2); color: var(--fh-bg); border: none;
  border-radius: var(--fp-radius-sm); padding: 0.6rem 1rem;
  font-weight: 600; font-size: 0.9rem; cursor: pointer; white-space: nowrap;
}
.fh-check-btn:hover { filter: brightness(1.08); }
.fh-check-result { margin: 0 0 1rem; }
.fh-check-dim { font-size: 0.9rem; color: var(--fh-muted); font-style: italic; }
.fh-check-hit {
  display: flex; justify-content: space-between; align-items: center; gap: 0.75rem;
  padding: 0.7rem 0.85rem; background: var(--fh-bg);
  border: 1px solid var(--fh-accent2); border-radius: var(--fp-radius-sm);
  text-decoration: none; color: var(--fh-text);
}
.fh-check-hit:hover { filter: brightness(1.06); }
.fh-check-name { display: flex; flex-direction: column; font-size: 0.96rem; font-weight: 600; }
.fh-check-line { font-size: 0.76rem; font-weight: 400; color: var(--fh-muted); margin-top: 1px; }
.fh-check-price { display: flex; flex-direction: column; align-items: flex-end; font-family: var(--fh-display); font-size: 1.05rem; color: var(--fh-hot); white-space: nowrap; }
.fh-check-sample { font-family: var(--font-body); font-size: 0.7rem; font-weight: 400; color: var(--fh-muted); margin-top: 1px; }

.fh-filter { display: flex; flex-wrap: wrap; gap: 0.4rem; margin: 0 0 0.9rem; }
.fh-filter-pill {
  background: var(--fh-bg); border: 1px solid var(--fh-line);
  border-radius: 999px; padding: 0.3rem 0.8rem;
  color: var(--fh-muted); font-size: 0.8rem; font-weight: 600; cursor: pointer;
  transition: color var(--transition), border-color var(--transition), background var(--transition);
}
.fh-filter-pill:hover { color: var(--fh-text); border-color: var(--fh-accent2); }
.fh-filter-pill.is-active { background: var(--fh-accent2); color: var(--fh-bg); border-color: var(--fh-accent2); }
.fh-filter-pill:focus-visible { outline: 2px solid var(--fh-accent); outline-offset: 1px; }

@media (max-width: 520px) {
  .fh-check-btn { flex: 1 1 100%; }
}

/* ── MOTU Power Sword vaults — sealed→drawn line sections ── */
.fh-vaults { margin: 2.5rem 0; }
.fh-vaults-head { margin-bottom: 1.1rem; }
.fh-vaults-title { font-family: var(--fh-display); font-size: 1.6rem; letter-spacing: 0.02em; color: var(--fh-text); margin: 0 0 0.3rem; }
.fh-vaults-sub { font-size: 0.95rem; color: var(--fh-muted); margin: 0; }

.fh-vault { border: 1px solid var(--fh-line); border-radius: var(--fp-radius); margin-bottom: 0.6rem; background: var(--fh-surface); overflow: hidden; transition: border-color var(--transition); }
.fh-vault.is-open { border-color: var(--fh-accent2); }

.fh-vault-seal {
  width: 100%; display: flex; justify-content: space-between; align-items: center; gap: 1rem;
  background: transparent; border: none; cursor: pointer; text-align: left;
  padding: 1rem 1.15rem; color: var(--fh-text);
}
.fh-vault-seal:hover { background: var(--fh-bg); }
.fh-vault-seal:focus-visible { outline: 2px solid var(--fh-accent); outline-offset: -2px; }
.fh-vault-meta { display: flex; flex-direction: column; gap: 0.15rem; min-width: 0; }
.fh-vault-line { font-family: var(--fh-display); font-size: 1.2rem; letter-spacing: 0.02em; color: var(--fh-text); }
.fh-vault.is-open .fh-vault-line { color: var(--fh-hot); }
.fh-vault-era { font-size: 0.74rem; letter-spacing: 0.04em; color: var(--fh-accent2); font-family: var(--font-mono); text-transform: uppercase; }
.fh-vault-teaser { font-size: 0.9rem; color: var(--fh-muted); font-style: italic; margin-top: 0.2rem; }

.fh-sword { display: flex; flex-direction: column; align-items: center; gap: 0.25rem; flex-shrink: 0; color: var(--fh-accent2); }
.fh-sword-label { font-size: 0.6rem; letter-spacing: 0.12em; font-family: var(--font-mono); color: var(--fh-accent2); white-space: nowrap; }
.fh-sword-svg { display: block; transform: translateY(6px) rotate(0deg); transition: transform 420ms cubic-bezier(.2,.9,.3,1.3), filter 420ms ease; }
.fh-vault.is-open .fh-sword-svg { transform: translateY(-2px) rotate(0deg); filter: drop-shadow(0 0 6px var(--fh-hot)); color: var(--fh-hot); }
.fh-vault-seal:hover .fh-sword-svg { filter: drop-shadow(0 0 4px var(--fh-accent2)); }

.fh-vault-body { padding: 0 1.15rem 1.15rem; animation: fh-unseal 360ms ease; }
@keyframes fh-unseal { from { opacity: 0; transform: translateY(-6px); } to { opacity: 1; transform: none; } }
.fh-vault-lore { font-size: 1rem; color: var(--fh-muted); line-height: 1.75; margin: 0 0 1rem; }
.fh-vault-grails-label { font-size: 0.7rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--fh-accent2); font-family: var(--font-mono); margin-bottom: 0.5rem; }
.fh-vault-chips { display: flex; flex-wrap: wrap; gap: 0.45rem; }
.fh-vault-chip {
  display: flex; flex-direction: column; gap: 0.1rem;
  background: var(--fh-bg); border: 1px solid var(--fh-line); border-radius: var(--fp-radius-sm);
  padding: 0.5rem 0.7rem; text-decoration: none; color: var(--fh-text); font-size: 0.86rem; font-weight: 600;
  transition: border-color var(--transition);
}
.fh-vault-chip:hover { border-color: var(--fh-accent2); }
.fh-vault-chip-name { white-space: nowrap; }
.fh-vault-chip-price { font-family: var(--fh-display); font-size: 0.95rem; color: var(--fh-hot); display: flex; align-items: baseline; gap: 0.4rem; }
.fh-vault-chip-sold { font-family: var(--font-body); font-size: 0.68rem; font-weight: 400; color: var(--fh-muted); }
.fh-vault-chip-all { justify-content: center; color: var(--fh-accent); font-weight: 600; border-style: dashed; }
.fh-vault-chip-all:hover { color: var(--fh-text); }

/* First sword drawn powers up the page — Grayskull glow intensifies once */
.fh-vaults.is-powered .fh-vaults-title { text-shadow: 0 0 14px rgba(240,185,66,0.25); }

@media (prefers-reduced-motion: reduce) {
  .fh-sword-svg, .fh-vault.is-open .fh-sword-svg { transition: none; }
  .fh-vault-body { animation: none; }
}
@media (max-width: 520px) {
  .fh-vault-chip-name { white-space: normal; }
  .fh-sword-label { display: none; }
}
